# MIP-<%= responses.mipNumber %> Rebalancing of <%= config.networkName %> Token Liquidity Incentives

The following proposal was generated by community member `<%= responses.name %>` using the moonwell market rebalance tool v`<%= _meta.generatorVersion %>` on `<%= new Date().toUTCString() %>`, which used data from the `<%= config.networkName %>` network at block height `<%= snapshotBlock.toLocaleString() %>`.
